# Node
node_modules/
packages/ts/dist/
packages/ts/src/index.ts
*.tsbuildinfo

# Rust
target/
Cargo.lock

# Generated artifacts (regenerated by `make gen`)
# generated.rs now lives in OUT_DIR (not src/), but keep ignored in case of stale files.
crates/qua-media-contracts/src/generated.rs
# schemas/ inside the crate dir are committed (bundled copy for registry publish).
# Do NOT add crates/qua-media-contracts/schemas/ here.

# Editor
.vscode/
.idea/
.DS_Store
